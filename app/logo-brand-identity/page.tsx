import type { Metadata } from 'next';
import { LogoBrandIdentityPage } from '../../components/category/LogoBrandIdentityPage';

export const metadata: Metadata = {
  title: 'Logo & brand identity — Khoa',
};

export default function Page() {
  return <LogoBrandIdentityPage />;
}
